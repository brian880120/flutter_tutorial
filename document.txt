if column, children: <Widget>[]
if Widget, child
container(
    margin: EdgeInsets.all(10.0),
    child: Widget
)

access statefull widget
void initState() {
    widge.startingProduct
    super.initState();
}

ListView.builder(
    itemBuilder: () {},
    itemCount: int,
)

57